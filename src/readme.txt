Cette application est un test de portfolio qui permet d'être réutilisablr en changeant le nom l'adresse github et toute information personnelle propre à chacun.

Il est important de noter qu'il y a des problèmes dans le code que le créateur n'arrive pas à régler. Le rendu dans le navigateur est malheureusement un page blanche, avec des erreurs dans la console.